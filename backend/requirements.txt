# Runtime dependencies — pinned for reproducible production builds.
# Comparator libs pinned to the LATEST resolved versions (what the colleague's unpinned
# `>=` set installs), so issue counts match their environment while staying reproducible.

# --- Web API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Comparator pipeline ---
PyMuPDF==1.27.2.3
numpy==2.5.0
scipy==1.18.0
scikit-image==0.26.0
Pillow==12.2.0
PyYAML==6.0.3
Jinja2==3.1.6

# --- Robust text-matching layer (colleague's backend) ---
# opencv-python-headless (not opencv-python): same cv2 API but no libGL system dep,
# so it imports cleanly on the headless EC2.
opencv-python-headless==4.13.0.92
pandas==3.0.3
scikit-learn==1.9.0
